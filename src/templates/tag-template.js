import { Layout } from "../components"
import React from "react"

export default function TagTemplate({ location }) {
  return (
    <Layout location={location}>
      Template
      {/*  */}
    </Layout>
  )
}
